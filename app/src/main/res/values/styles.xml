<resources
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:itemBackground">@color/cardColor</item>
        <item name="android:textColor">@color/white</item>
        <item name="actionOverflowButtonStyle">@style/ThreeDotColor</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- ===============   navigation drawer horizontal line color   ================= -->
    <style name="NavigationDrawerStyle">
        <item name="android:listDivider">@color/navigationHorizontalLineColor</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColorSecondary">@color/navigationHorizontalLineColor</item>
    </style>

    <!--=================== Option Menu Three dot color =====================-->
    <style name="ThreeDotColor" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/white</item>
    </style>

  <!--== facebook login activity setup ==-->

    <style name="SwitchStyle">
        <item name="colorControlActivated">#f19066</item>
        <item name="colorControlNormal">@color/textColor1</item>
    </style>

    <style name="AutoCompleteStyle" parent="AppTheme">
        <item name="colorAccent">@color/textColor1</item>
    </style>

    <style name="EditProfileStyle" parent="AppTheme">
        <item name="colorAccent">@color/textColor4</item>
    </style>

    <style name="SearchViewStyle" parent="AppTheme">
           <item name="colorAccent">@color/textColor1</item>
    </style>

    <style name="SheetDialog" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!--<item name="android:backgroundDimAmount">0.3</item>-->
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="ProfileStyle" parent="AppTheme">
        <item name="colorPrimaryDark">@color/cardColor</item>
    </style>

    <style name="ProgressDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColorPrimary">@color/textColor3</item>
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- ===============     This is for popup window animation    ================= -->
    <style name="windowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

   </resources>

